name: Flutter Auto Grading

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.13.0'  # Use your required version

      - name: Install dependencies
        run: flutter pub get

      - name: Run tests
        run: flutter test
